<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme: dark splash background -->
    <style name="Theme.UntilDone" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/splash_dark</item>
        <item name="android:statusBarColor">@color/splash_dark</item>
        <item name="android:navigationBarColor">@color/splash_dark</item>
    </style>
</resources>
